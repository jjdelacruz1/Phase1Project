{
  "scripts": {
    "test": "npm standard",
    "deploy": "rm -rf ./dist && rsync -r ./ ./dist && cp ./dist/homepage.html ./dist/200.html && surge ./dist --domain http://happyhourfinder.surge.sh"
  },
  "devDependencies": {
    "standard": "13.1.0"
  },
  "standard": {
    "globals": [ "$", "L", "sessionStorage", "ajaxResponse", "map", "searchTerm", "Bideo", "markersArray", "deleteMarkers", "insertHtmlIntoContainer", "yelpApiKey", "corsAnywhereUrl", "map", "new_height"]
  }
}
